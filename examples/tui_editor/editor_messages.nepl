#indent 4
#target wasix

fn msg_ready <()*>str> ():
    "ready"

fn msg_saved <()*>str> ():
    "saved"

fn msg_save_failed <()*>str> ():
    "save failed"

fn msg_run_hint <()*>str> ():
    "run command is shown on right pane"

fn msg_no_word <()*>str> ():
    "no word on cursor"

fn msg_edited <()*>str> ():
    "edited"
