{
  "name": "neplg2-playground",
  "version": "0.1.0",
  "description": "Web playground for NEPLg2 programming language",
  "scripts": {
    "start": "npm run copy && npx serve .",
    "copy": "mkdir -p examples && cp ../examples/*.nepl examples/",
    "build": "npm run copy && trunk build --release"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
