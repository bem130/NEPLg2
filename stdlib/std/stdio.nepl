#indent 4

#if[target=wasi]
#extern "wasi_snapshot_preview1" "fd_write" fn fd_write <(i32,i32,i32,i32)->i32>

#if[target=wasi]
#import "std/mem"
#use std::mem::*
#if[target=wasi]
#import "std/string"
#use std::string::*

#if[target=wasi]
fn print_str <(str)*>()> (s):
    ()

#if[target=wasi]
fn print_i32 <(i32)*>()> (v):
    ()
