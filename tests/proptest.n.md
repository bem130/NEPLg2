# proptest.rs 由来の doctest

このファイルは Rust テスト `proptest.rs` を .n.md 形式へ機械的に移植したものです。移植が難しい（複数ファイルや Rust 専用 API を使う）テストは `skip` として残しています。
## prop_add_commutative

neplg2:test[skip]
```neplg2
#entry main
#indent 4
fn main <()->i32>():
    0
```

## prop_sub_inverse

neplg2:test[skip]
```neplg2
#entry main
#indent 4
fn main <()->i32>():
    0
```

## prop_fail_example

neplg2:test[compile_fail,skip]
```neplg2
#entry main
#indent 4
fn main <()->i32>():
    0
```
